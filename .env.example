# typrun-ws 환경변수 (.env 로 복사해서 사용 — .env 는 git 제외)
PORT=3001

# 세션 JWT 검증 시크릿 — 운영(prod)에선 typrain-server 의 JWT_SECRET 와 반드시 동일하게.
# 비우면 dev 폴백('dev-insecure-...-32')을 typrain-server 와 똑같이 써서 로컬은 바로 동작.
JWT_SECRET=

# typrain-server 가 발급하는 세션 쿠키 이름 (기본 typrun_session)
SESSION_COOKIE=typrun_session
